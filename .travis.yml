langage : node_js
node_js: 12

stages:
  - lint

jobs:
  include:
  - name: Eslint
  stage: lint
  install: npm install
  script: npm run build

  stages: 
    - trasnpile

  jobs:
    include:
    - name: Transpile
      stage: transpile
      install: npm install
      script: npm run build